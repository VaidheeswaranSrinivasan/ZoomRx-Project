Please follow the steps in the app servers
============================================

1) initializing the swarm mode for Docker

    docker swarm init

2) Join the node 2 as a worker swarm cluster using the token

3) Create a docker network using the "overlay" driver

    docker network create -d overlay my-app-network

9) Create the service for for the application

    docker service create --name app -p 80:80 --replicas 2 --network my-app-network nigelpoulton/pluralsight-docker-ci